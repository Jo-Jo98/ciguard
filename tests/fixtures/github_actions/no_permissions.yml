# Targeted fixture for GHA-IAM-005 (no permissions: declared anywhere).
#
# Everything else is fine — single trivial job, SHA-pinned action, push-only
# trigger. The only finding we expect is GHA-IAM-005.

name: no-perms

on:
  push:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - run: echo "hello"
