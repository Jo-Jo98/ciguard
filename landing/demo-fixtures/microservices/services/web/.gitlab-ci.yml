stages: [test, build, deploy]

test-web:
  stage: test
  image: node:20-alpine
  script: ['npm test']

build-web:
  stage: build
  image: docker:25.0
  script:
    - docker build -t web .

deploy-web:
  stage: deploy
  image: alpine:3.19
  script: ['echo deploy web']
  when: manual
