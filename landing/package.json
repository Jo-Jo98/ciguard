{
  "name": "ciguard-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Landing page for ciguard.dev — built with Astro, deployed on Cloudflare Pages.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build:demos": "python3 scripts/build_demos.py",
    "build:screenshots": "node scripts/build-screenshots.mjs",
    "build:assets": "npm run build:demos && npm run build:screenshots",
    "build:all": "npm run build:assets && npm run build"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
