# Screenshot PNGs are generated by `landing/scripts/build-screenshots.mjs`
# from the HTML artifacts written by `build_demos.py`. The CI workflow
# regenerates them on every build so they reflect the current engine
# output (visualiser changes → screenshots refresh automatically).
*.png
