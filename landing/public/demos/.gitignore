# Demo HTML artifacts are generated by `landing/scripts/build_demos.py`
# from `landing/demo-fixtures/`. The CI workflow regenerates them
# before every Astro build so they can never go stale relative to the
# shipped engine version.
*.html
